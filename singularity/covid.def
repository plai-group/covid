Bootstrap: localimage
From: covid_base.sif

%files

%post
export CC=gcc-5
export CXX=g++-5
export PYTHON_VERSION=2.7
export PATH=/usr/bin:$PATH
# env-python
export PATH=/opt/conda/bin:$PATH
export LANG=C.UTF-8
export LC_ALL=C.UTF-8

# pyprob_cpp
cd /code && git clone --branch master https://github.com/probprog/pyprob_cpp.git && cd pyprob_cpp && mkdir build && cd build && cmake ../src && cmake --build . && make install

# pyprob
cd /code && git clone https://github.com/probprog/pyprob.git && cd pyprob && pip install .	


# Sacred
pip install sacred
pip install pymongo


pip install seaborn
pip install h5py scipy ujson


export LD_LIBRARY_PATH=/usr/local/lib/:$LD_LIBRARY_PATH

%environment
export LD_LIBRARY_PATH=/usr/local/lib/:$LD_LIBRARY_PATH
